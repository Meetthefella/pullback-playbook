KLAC benchmark intent

- Semantic target: a true 50MA support-test case, not an off-level continuation story.
- The benchmark should fail any narration that claims price is away from support while scanner context still classifies the chart as `Pullback Near 50MA`.
- Buyer response is intentionally unproven here. Support context should survive without being inflated into rebound confirmation.
